---
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{include "standard-base.name" $}}
  labels: {{include "standard-base.labels" $ | nindent 4}}
spec: {{ include "standard-base.yaml" (omit .Values "component" "global" "name" "template") | nindent 2 }}
  jobTemplate:
    spec:
      template:
        metadata:
          labels: {{include "standard-base.labels" $ | nindent 12}}
        spec: {{include "standard-base.pod" $ | nindent 10 -}}
