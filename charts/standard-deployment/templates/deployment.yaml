---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{include "standard-base.name" $}}
  labels: {{include "standard-base.labels" $ | nindent 4}}
spec:
  replicas: {{.Values.replicas}}
  selector:
    matchLabels: {{include "standard-base.selectors" $ | nindent 6}}
  template:
    metadata:
      labels: {{include "standard-base.labels" $ | nindent 8}}
    spec: {{include "standard-base.pod" $ | nindent 6 -}}
